# ov5640_hdmi_1280x720

本例程内容与 `ov5640_hdmi_640x480` 项目基本一致，区别是将视频输出分辨率由 640x480 修改为 1280x720。

## 主要改动

1. `video_driver` 模块中，将 480p 的 VGA 时序修改为 720p 时序。
2. `ov5640_cfg` 模块中，将 OV5640 初始化配置修改为 1280x720p。
3. 顶层模块中，将 SDRAM 的工作时钟统一修改为 125 MHz，将 HDMI 的工作时钟修改为 75 MHz。

除此之外，其余部分与 640x480 项目内容一致。

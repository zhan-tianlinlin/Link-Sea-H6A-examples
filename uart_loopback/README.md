# 串口回环例程

## 功能

该项目实现 UART 串口回环：上位机通过串口发送数据到 FPGA，FPGA 接收后再通过串口发送模块原样发回，用来验证串口发送和接收模块是否正常工作。

你们可以拿里面的发送和接收模块去干别的事<br>
虽然这个例程很简单，我也顺便弄一个给你们这些比较懒的 baka 吧<br>
!发现我的以太网回环案例的英文写错了，lookback 是个啥，算了，不管了，我是 baka
